import number_service
"""
When you import a file, it runs all
the cocde in that file. This means if
the other file has runable code, that also
executes as well

"""
print(number_service.number)
print(number_service.is_prime(15))

print("Name in week6 " + __name__)
print(number_service.is_prime(15))